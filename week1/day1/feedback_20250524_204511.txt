URL: https://github.com/emilypeng2025/DI-Bootcamp/blob/main/week1/day1/dailychallenge.py
suggestions for improvement:
- The prompt asks for a string of EXACTLY 10 characters.  The current code accepts any string and then checks the length. It should enforce the 10-character limit before proceeding.
- Improve variable names for better readability (e.g., `user_input` instead of `string`).
- Add comments explaining the purpose of each section of the code, especially the loop that builds the string character by character.
- The output of the character-by-character build could be improved by using a single print statement with string manipulation rather than multiple prints in the loop.
Brief justification:
- correctness: The code correctly implements the length check, printing the first and last characters, building the string character by character, and shuffling the string (bonus). However, it doesn't strictly enforce the 10-character requirement as the prompt dictates. It checks the length afterward, which is less robust.  The core functionality for steps 3 and 4 aligns with the chapter's requirements on conditionals and loops.
- readability: The code is generally understandable, but variable names could be more descriptive.  Adding comments to explain the logic in certain sections, particularly the character-building loop, would significantly improve readability.
- performance: The code's performance is efficient. The algorithms used for string manipulation and shuffling are optimal for their respective tasks. There are no obvious performance bottlenecks.
- security: The code does not introduce any security vulnerabilities.  It only handles user input and performs string manipulations; no external resources or sensitive data are involved.

