URL: https://github.com/emilypeng2025/DI-Bootcamp/blob/main/week1/day1/dailychallenge.py
suggestions for improvement:
- The prompt asks for a string of exactly 10 characters. The code accepts any string length and provides feedback, but doesn't enforce the 10-character limit.  The program should explicitly enforce this requirement by only proceeding if the length is exactly 10.
- Add more descriptive variable names (e.g., instead of `string`, use `user_input_string`).
- The comments could be more detailed and informative, explaining the purpose of each code section more thoroughly.
- Consider using a more robust method for handling potential errors, such as a `try-except` block to manage potential `IndexError` if the input is not a string or is empty.
Brief justification:
- correctness: The code correctly implements the length check, printing of first and last characters, character-by-character string building, and string shuffling (bonus).  However, it does not strictly enforce the 10-character length requirement as instructed.  It also fails to handle cases where an empty string or non-string is entered by the user.  The rest of the functionalities match the requirements outlined in the chapter content, leading to a 95% score.
- readability: The code's structure is generally clear. However, more descriptive variable names and improved comments would enhance readability significantly.  The current variable names are somewhat vague, making it slightly harder to understand the code's purpose at a glance.
- performance: The code's performance is efficient for the tasks involved. The loops and string manipulations are implemented effectively without unnecessary overhead.
- security: The code does not introduce any security vulnerabilities.  The only interaction is user input, which is handled without any potential security risks.

